/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // EngineeringAdvisor · Next.js 14 App Router 配置
  // 2026-08-26 v0.1 · 由 29-工程 cron 脚手架生成
};

export default nextConfig;
