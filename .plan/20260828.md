# 2026-08-28 · 每日增量

> T4 每日增量 · 不修改主计划 `项目开发计划.md` 既有章节(仅 checkbox 状态变更)

## 今日工作

第 4 日 T4 日增量。**今日按 `.plan/20260827.md` 第三节"0828 候选 A"** 执行,目标:基础目录结构 5 个目录,后端补全,不复用 Next.js 的 `app/`。

**实际产出 1 个小变更:5 个目录 × (`__init__.py` + `README.md`) = 9 文件手写**(与 0826/0827 文件风格一致)。

涉及文件:
- `api/lib/__init__.py` (新增,579 B) — 通用工具层包标记 + "禁止 import services/routers"边界说明
- `api/lib/README.md` (新增,993 B) — 职责/不做什么/扩展点 5 行
- `api/services/__init__.py` (新增,816 B) — 业务服务层包标记 + 上下游关系图
- `api/services/README.md` (新增,1348 B) — 按 Phase 0/1/2/3 推进的 6 个预留子模块表
- `api/routers/__init__.py` (新增,755 B) — HTTP 路由层包标记
- `api/routers/README.md` (新增,1104 B) — 薄壳定位 + 路由切分表
- `tests/__init__.py` (新增,508 B) — pytest 测试根包标记
- `tests/README.md` (新增,1129 B) — 目录布局 + 首个测试用例建议
- `docs/README.md` (新增,1020 B) — 工程文档根,工具选型留 0829 拍板
- `项目开发计划.md` (更新 1 个 checkbox:第 9 节第 4 项 `- [ ]` → `- [x]`)
- `.plan/20260828.md` (新建,本文件) — 记录今日过程

目录结构与昨日 plan 第三节"0828 候选 A"完全对齐:
- `api/lib/`     通用工具(后续 logging/exception/retry)
- `api/services/` 业务服务层(Phase 0 飞书 bot → Phase 3 可靠性)
- `api/routers/`  路由层(按业务切分,薄壳)
- `tests/`         pytest 根目录(镜像 api/ 结构)
- `docs/`          工程文档(MkDocs/Sphinx 待 0829 拍板)

## 今日发现

1. **`.plan/20260827.md` 第三节 A 项建议被完整采纳**:`api/lib/ api/services/ api/routers/ tests/ docs/` 5 个目录与昨日 plan 1:1 对齐,**未误起 `app/` 目录**(0828 巡检报告第二节第 2 项风险提醒)。
2. **`api/lib/` 与 `api/core/` 的边界首次显式定义**:`core` 是"配置中心"(settings/db 连接池),`lib` 是"工具箱"(logging/retry/exception)。这两个目录在 0827 已有 `core/`,0828 补全 `lib/` 后,后端 4 个职责层清晰:**`main`(工厂) + `core`(配置) + `lib`(工具) + `services`(业务) + `routers`(HTTP)**。
3. **路由层与 main.py 的关系需 0829 决定**:现有 3 个端点 `/` `/healthz` `/api/v1/info` 仍内联在 `api/main.py:create_app()` 内,0829 README 升级时建议**拆出到 `api/routers/meta.py`**,与未来 `/api/v1/advisory` 等业务路由风格一致。
4. **首个 pytest 用例位置已暗示**:`tests/README.md` 建议首个用例是 `test_api_main.py` 验证 3 端点,作为 0827 FastAPI 骨架的回归保护 —— 但**本任务不写实际测试**(0829 后再写,保持"1 个小变更"边界)。
5. **第 9 节"工程基础脚手架" 推进至 4/5**:0825 `.gitignore` ✓ → 0826 Next.js 6 文件 ✓ → 0827 FastAPI 7 文件 ✓ → 0828 5 目录 9 文件 ✓;**剩 0829 仓库根 README 升级**(从 483 B 占位 → 含 quickstart + 架构图),**0829 是 5/5 脚手架完整日,0830 起可启动 Phase 0**。
6. **docs/ 工具选型留 0829 拍板**:`MkDocs+Material` / `Sphinx+MyST` / `Docusaurus` 三选一,0829 仓库根 README 升级时一并决定(因为 README 可能直接用 docs/ 的工具构建)。

## 验收

- [x] 5 个目录落地:`api/lib/` `api/services/` `api/routers/` `tests/` `docs/`
- [x] 每个目录含 `__init__.py` 包标记 + `README.md` 职责说明
- [x] 9 文件,4 KB,手写不跑 install
- [x] 与昨日 plan 第三节 A 项 1:1 对齐
- [x] 未误起 `app/` 目录(避免与 Next.js 冲突)
- [x] 主计划前 8 节未修改(只更新第 9 节第 4 项 checkbox)
- [x] `.plan/20260828.md` 记录今日过程
- [x] 显式 `git add` 9 个文件,不带 `工程顾问开发架构与计划.md`(仍 untracked 5 日)
- [x] `.plan/20260825.md` `.plan/20260826.md` `.plan/20260827.md` 仍 tracked(本任务不擅自 `.gitignore`)

## 下一日(2026-08-29)候选

按第 9 节"工程基础脚手架"清单收尾:
- **A**(主,planned for 2026-08-29):仓库根 `README.md` 升级(483 B 占位 → 含 quickstart + 架构图),0830 脚手架 5/5 完整日
- B:`tests/test_api_main.py` 首个 pytest 用例(验证 3 端点),作为 0827 骨架回归保护
- C:`.env.example` Pydantic Settings 字段全枚举(供团队接入文档)
- D:`api/routers/meta.py` 拆出 main.py 内联端点,与未来业务路由风格一致

**A 的细节建议**:
- 顶部:项目一句话定位(技术决策上下文 + 团队知识沉淀)
- Quickstart:clone → pnpm i → pnpm dev(Next.js 端) + uv sync → uvicorn api.main:app(FastAPI 端)
- 架构图:复用主计划第 4 节 ASCII 架构图
- 路线图:主计划第 7 节里程碑表
- 链接:`项目开发计划.md` / `.Log/巡检-工程-{date}.md` 列表

## 备注

- 本次 commit 主题:`scaffold: 基础目录结构 v0.1 - 5 目录 9 文件手写(后端补全)` (与 0826/0827 风格一致)
- 显式 `git add` 9 个新文件,不 `git add -A`
- `工程顾问开发架构与计划.md` 仍 untracked(5 日),等用户决策
- `.plan/` 目录定位待 T1 拍板:本任务仍不擅自 `.gitignore`
- 0828 巡检报告建议的"`app/` 冲突"风险**已规避**(本任务用了 `api/lib/services/routers` 而非 `app/`)
