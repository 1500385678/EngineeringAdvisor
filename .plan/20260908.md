# 2026-09-08 · 每日增量

> T4 每日增量 · 不修改主计划 `项目开发计划.md` 既有章节(本次无 Phase 0/1 推进,仅索引)

## 今日工作

**`.plan/20260908.md` 由本任务自生成** —— 0828 至今 .plan/ 目录无新文件(0828-0908 共 12 日),0908 巡检(02:40 入库 `278880e`)第 1/3/4 项已明确"T1 任务进入'环境维护/纯闭环'模式,0827 plan 4 候选 100% 闭合后无现成 plan 候选,连续 4 日 0 业务 commit"。本任务**回退到 0827 同款处理**:读巡检报告 → 选最小修缮变更 → 不擅自启动 Phase 0 第 2 步(那是 T1 拍板项)。

**实际产出 1 个小变更:写 `docs/phase0/00-README.md` 索引**,0831 `a50ed14` 资产盘点产物已 8 日无导航/无索引,0905 巡检(第 1 项)已建议"T1 立即拍板 3 项优先级 (a)/(b)/(c)",本任务不替 T1 拍板,但**给 T1 拍板提供 1 份集中决策素材**(路径 a/b/c + 5 关键发现摘要),与 0905 `.gitignore` 修补同性质("环境维护/闭环已有信息")。

涉及文件:
- `docs/phase0/00-README.md` (新建,本文件) — Phase 0 资产盘点目录索引,2.4 KB
- `.plan/20260908.md` (新建,本文件) — 记录今日过程

**不触动**:
- 主计划 `项目开发计划.md` 任何 checkbox(本次无 Phase 0/1 推进)
- `工程顾问开发架构与计划.md` 19.5 KB 文档(连续 15 日 untracked,0827 T1 决策"不主动入库"持续生效,本任务不擅自操作)
- `.plan/202608{25,26,27}.md` 3 份历史(14 次巡检连续建议"入 .gitignore 或转 .Log/",0827 T1 决策"留待 T1 拍板"未闭环,本任务不擅自 `.gitignore`)

## 今日发现

1. **0828 至今 .plan/ 中断 12 日,与 0827 plan 写后清空形成"无 .plan 候选"循环**:0827 是最后一份手写 .plan(0828-0908 期间 T4 任务有 commit 但未写 .plan,0906 `a889409` 是"步骤 9 删前 1 日 .plan"模式 —— 即先写 .plan/20260906.md,T4 跑完步骤 9 删除);**新观察**:本任务尝试"自生成 .plan"是 0827 同款处理(巡检建议 → 自生成 → 最小变更),但**自生成 .plan 不是可持续模式** —— 0827 之后 12 日未走"自生成"路径,可能 T1 任务更倾向于"无 .plan 步骤 9 删空"模式;**本任务保留 .plan/20260908.md 入库**(不步骤 9 删空),作为"自生成模式"再次启动的可见信号,等 T1 拍板是否长期保留。
2. **T1 任务"环境维护/纯闭环"模式延续 4 日(0905/0906/0907/0908),本次变更与 0905 同性质**:0905 `5434c9d` 修补 .gitignore 9 日挂账是首个"环境维护/隐患修复" commit,0906 `a889409` 是"状态记录 + 删 .plan" 1-commit-2-file 模式,0907 `903cb75` 仅巡检入库,0908 0 commit;**本次 `00-README.md` 索引是"信息修缮"模式** —— 0831 资产盘点产物 7.9 KB 已 8 日无导航,T1 拍板时需快速看到"现状是什么 + 3 路径选哪个",索引是修缮工作区,不动主计划。
3. **路径 (a) "5-7 工作日工作量"在 0.08 项/工作日速度下不可达**:巡检第 1 项已计算 11 项剩需 138 工作日(180+ 工作日)远超 0912 节点;**路径 (a) 单独 5-7 天就占 5-7 工作日**,在 4 工作日窗口(0908-0911)内不可能完成;**索引里 3 路径都标"工作日"成本,让 T1 拍板时一眼看出可行性**:路径 (a) 5-7 日 vs 路径 (b) 1+3-5=4-6 日 vs 路径 (c) 双线 6-9 日(并行缩短但需 T1 投入双倍时间)。
4. **0831 资产盘点产物的"6 个相关文档"指向全断(归档已 8 日)**:文档末尾"六、关联文档"列了 `[[项目开发计划]]` `[[../README|工程 README]]` `.plan/20260831.md` `.Log/巡检-工程-20260831.md`,但 `.plan/20260831.md` 早已步骤 9 闭环(0905 巡检已说明 0825/26/27 3 份历史未消化),`.Log/巡检-工程-20260831.md` 已存在但 git status untracked 数仍是 15 日;**索引用 Obsidian 风格双链 `[[../README|工程 README]]`** 维持 0831 风格,即使外部文件移动,链接断也只影响跳转不影响渲染。
5. **"为已有产物加索引"是低成本闭环模式,可作为后续 T4 任务默认动作**:0825 启动以来 .Log/ 14 份巡检 + 6 份状态记录已有,但无 README 索引,0831 资产盘点产物 7.9 KB 已有但无索引,0829 README 7.9 KB 升级版已有但 0906 加"项目状态"后 10.5 KB 仍无 phase0/ 索引;**今日为 `docs/phase0/` 加 `00-README.md` 索引,可推广到 `.Log/` `.plan/` `docs/adr/`(待建)`api/` `tests/` 等子目录** —— 但本任务不擅自推广,只给 T1 拍板建议"是否将'子目录加 00-README.md 索引'作为 T4 任务默认动作"。

## 验收

- [x] `docs/phase0/00-README.md` 新建,2.4 KB 左右,2 节(关键发现 + 3 路径待 T1 拍板)
- [x] 引用 0831 资产盘点产物 `01-资产盘点-10子目录概览.md`,关键发现摘要(10 目录非软件工程)
- [x] 3 路径 (a)/(b)/(c) 列全 + 工作日成本估算,供 T1 拍板用
- [x] 主计划前 8 节未修改(第 5/6 节 1/12 进度不变)
- [x] `.plan/20260908.md` 记录今日过程
- [x] `工程顾问开发架构与计划.md` 仍 untracked(不主动入库,等 T1 拍板)
- [x] `.plan/202608{25,26,27}.md` 3 份历史不动(等 T1 拍板)

## 下一日(2026-09-09)候选

按"环境维护/纯闭环"模式自然推进(0827 plan 4 候选闭合后无现成 plan 候选,模式延续):

- **A**(优先,延续 0908 模式):为 `.Log/` 加 `00-README.md` 索引 —— 14 份巡检 + 6 份状态记录已有但无目录索引,0908 巡检第 1/3/4/5 项已说明巡检模式稳定可索引
- **B**:为 `api/` 加 `00-README.md` 索引 —— 0828 `63281bb` FastAPI 骨架 13 文件 + 0827 plan 4 候选 100% 闭合,目录结构 `api/{__init__,main}.py` + `api/{core,lib,services,routers,models}/` 已稳定
- **C**:为 `tests/` 加 `00-README.md` 索引 —— 0903 `b032288` 1 文件 + 0827 plan B 项 5 日挂账落地
- **D**:`.env.example` 加注释(0904 `0303ed1` 1.2 KB 字段全枚举,加 CORS / log_level / debug 等使用说明)—— 0827 plan C 项延展
- **E**:`.plan/20260908.md` 决策:保留还是步骤 9 删空(本次任务步骤 9 会删,因本次是"自生成模式"试水,下一日回到 0906 模式"步骤 9 删空")

**T1 拍板建议**(本任务范围,优先级建议,不替 T1 决策):
1. **核心阻塞**:"无 .plan 候选 + 纯闭环"阶段 T4 任务来源拍板 — 切换到"为已有产物加 00-README.md 索引"模式(A/B/C 任一),还是"等待 T1 大计划"模式
2. **Phase 0 第 2-3 步路径拍板** — 路径 (a) 元方法论抽取 5-7 日 / 路径 (b) 新资料源 4-6 日 / 路径 (c) 混合 6-9 日,4 工作日窗口内只能选 (b) 或 (c)
3. **19.5 KB 架构文档入库归档** — 15 日 untracked 三选项:入 `docs/architecture.md` / 转 `docs/legacy/` / 删除
4. **`.plan/` → `.Log/plan-history/` 语义统一** — 14 次巡检建议,0827 T1 决策"留待 T1 拍板"未闭环

## 备注

- 本次 commit 主题:`docs: docs/phase0/00-README.md 索引 - 0831 资产盘点产物 8 日无导航小修缮` (与 0905 `.gitignore` 修补、0906 状态记录入库同性质"环境维护/纯闭环"模式)
- 本次 commit 同时带 `.plan/20260908.md` 新建(本任务自生成,0827 同款处理)
- 步骤 9 闭环:本次 T4 任务完成后**删除 `.plan/20260908.md`**(与 0906 模式一致,回退到"自生成模式试水 → 步骤 9 删空"循环)
- **手工显式 `git add` 文件,不 `git add -A`**:避免 19.5 KB untracked 误带上
- 0831 plan 后续动作(路径 a/b/c)8 日 T1 仍未拍板,本任务不擅自启动 Phase 0 第 2 步
- 0825 启动以来累计 36 commit(0908 02:40 巡检基准),本 commit 后预计 37 commit
