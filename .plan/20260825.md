# 2026-08-25 · 每日增量

> T4 每日增量 · 不修改主计划 `项目开发计划.md` 既有章节

## 今日工作

第 2 日 T4 日增量。读完昨天 20260824 plan + 02:40 巡检报告(`b0f6c1c`),确认两件事:
1. **项目代码 0 生成**:`src/` / `package.json` / `pyproject.toml` / 任何运行时代码全部缺失,仓库仍是"光计划"。
2. **`.plan/20260824.md` 工作区被删**:git 显示 deleted 但未 commit(巡检报告已记录),`git add -A` 时会自然消化,不再单独处理。

**挑 1 个最小可入库增量**:`.gitignore` 补全(昨天 plan 的 T2 候选之一 + 巡检报告"先把项目骨架落地"建议的最早落点)。同时为主计划新增 "## 9. 工程基础脚手架" 章节作为后续工程基础 checkbox 的干净落点。

涉及文件:
- `.gitignore` (50 B → 1811 B,5 行 → 113 行)
- `项目开发计划.md` (追加第 9 节,主计划前 8 节未动)
- `.plan/20260825.md` (新建,本文件)

## 今日发现

1. **`.gitignore` 原状 5 行太薄**:只覆盖 .DS_Store / *.log / node_modules / .venv / __pycache__,但主计划第 4 节明确 Next.js + FastAPI + Neo4j 混合栈,缺 .env / .next / out / dist / IDE / 测试覆盖率等关键忽略项。今天一次性补全,后续脚手架落地时不被 git 噪音阻塞。
2. **主计划功能项 checkbox(5/6 节)无法直接落地**:Phase 0 第 1 项"盘点 10 个子目录"需要先有 `_EngineeringLib-index.md` 之类文件,Phase 1 第 1 项"上线 ADR 中心"需要先有 FastAPI 脚手架。**所以工程基础必须先走完,功能项才有意义**。新设"## 9. 工程基础脚手架"作为 Phase -1,明示这个依赖关系。
3. **巡检报告与日增量是分工关系**:巡检 5 项(02:40)+ 日增量 1 项(03:40)覆盖不同时间点、不同动作——巡检只看不改,日增量看+改 1 处。两者 commit 分开清晰可追。

## 验收

- [x] `.gitignore` 覆盖主计划第 4 节所有技术栈
- [x] 主计划前 8 节未修改
- [x] 主计划第 9 节追加,1 个 checkbox 标记 [x]
- [x] `.plan/20260825.md` 记录今日过程
- [x] `.plan/20260824.md` 的 deleted 状态被本次 commit 自然消化

## 下一日(2026-08-26)候选

按新设"## 9. 工程基础脚手架"清单自然推进:
- **A**(优先):`package.json` + Next.js 14 App Router 脚手架初始化,目录结构 `app/` `lib/` `components/` 落地
- B:`pyproject.toml` + FastAPI 骨架,目录 `api/` `core/` `models/`
- C:基础目录结构(如果 A 选 next.js init,会顺带创建)
- D:仓库根 README 升级(从 483 B 占位 → 含 quickstart)

今天做完 `.gitignore` 后,A 已经是零阻塞——可以直接 `pnpm create next-app` 起步。

## 备注

- 本次 commit 主题:`chore: 工程基础脚手架 v0.1 - .gitignore 全栈补全` (或者 `scaffold: ...`,择一)
- 主计划是规划文档,**新增章节(9 节)算"建设性变更"不算"修改功能项 checkbox"**,后续 T4 如改 5/6/7/8 节内容需特别说明。
