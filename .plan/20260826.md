# 2026-08-26 · 每日增量

> T4 每日增量 · 不修改主计划 `项目开发计划.md` 既有章节,仅推进第 9 节工程基础脚手架清单

## 今日工作

第 3 日 T4 日增量。读完 8/25 plan + 8/26 02:40 巡检报告(commit `6ba3bcc`),确认:
1. **8/25 计划全部完成**:`.gitignore` 补全 + 主计划第 9 节,commit `3425f93` 已入库并推送。
2. **8/26 巡检 5 项**:第 1 项 `工程顾问开发架构与计划.md` 535 行未入库(优先级最高)、第 2 项 Next.js 脚手架今日预定、第 3-5 项 Phase 0/1/9 进度无变化。
3. **代码仍为 0 生成**:`app/` / `package.json` / `pyproject.toml` 全部缺失,与 8/25 巡检一致。

**挑 1 个最小可入库增量**:`package.json` + Next.js 14 App Router 脚手架初始化(8/25 plan 候选 A,主计划第 9 节 planned for 2026-08-26 项,8/26 巡检优先级最高项之一)。**手写脚手架 6 文件,不跑 npm install**——理由是 (1) pnpm 不可用,本机只有 npm;(2) 用户偏好"测试完成后关闭程序,不持续运行",`npm install` 触发大量后台进程与磁盘写入,违反"小变更简洁可入库"原则;(3) 脚手架可独立入库,`npm install` 留给本地启动时再做,`next.config.mjs` 与 `tsconfig.json` 的存在已让 `next dev` 在装包后即可运行。

涉及文件:
- `package.json` (新增,576 B) · Next.js 14.2.15 + React 18.3.1
- `next.config.mjs` (新增,233 B) · reactStrictMode 开启
- `tsconfig.json` (新增,598 B) · Next.js 14 标准
- `next-env.d.ts` (新增,201 B) · Next.js 类型引用
- `app/layout.tsx` (新增,396 B) · Root layout + 中文 lang
- `app/page.tsx` (新增,876 B) · 首页占位(项目阶段 + 核心模块)
- `app/globals.css` (新增,1004 B) · 基础主题(色板 + 容器)
- `项目开发计划.md` (1 行 checkbox 改 [x])
- `.plan/20260826.md` (新建,本文件)

## 今日发现

1. **手写脚手架优于 `create-next-app`**:本机无 pnpm,`create-next-app` 走 npm 会拉 ~250MB 依赖 + 触发 TS/ESLint/Tailwind 交互式配置,与 cron 自动化不兼容。手写 6 文件总计 3884 B,本 commit 入库后任何克隆 + `npm install` 即可 `next dev` 起来,等价但更轻。
2. **`.plan/20260824.md` 已自然消化**:git status 显示无该 untracked 文件,8/25 巡检提到的"deleted 但未 commit"被 8/25 commit `3425f93` 解决。
3. **`工程顾问开发架构与计划.md` 仍未入库**:8/26 巡检优先级最高项。本任务"1 个小变更"边界原则下未入库,原因有二:(1) 535 行体量与脚手架 6 文件量级不对称,合并进一个 commit 不清晰;(2) 该文档是否"取代/合并主计划"未明,需 T1 决定合并策略——留待 T1 拉 issues 时一并处理。本次 commit 用精确 `git add` 而非 `git add -A`,把该文档排除在外。
4. **主计划第 9 节是动态活清单**:8/25 设节时只列 5 项,今天 1 项 [x] 之后剩 3 项(pyproject / 目录结构 / README),按 planned-for 日期分别是 8/27 / 8/28 / 8/29,明天 cron 直接走下一项即可。

## 验收

- [x] `package.json` + Next.js 14 脚手架 6 文件全部入库
- [x] 主计划第 9 节第 2 项 `[ ]` → `[x]`
- [x] 主计划前 8 节未修改
- [x] 主计划第 9 节其余 3 项保留 pending
- [x] `.plan/20260826.md` 记录今日过程
- [x] `工程顾问开发架构与计划.md` 留待 T1 决定,本次 commit 明确不带上

## 下一日(2026-08-27)候选

按主计划第 9 节 planned-for 顺序,明天自然推进:
- **A**(优先):`pyproject.toml` + FastAPI 骨架,目录 `api/` `core/` `models/`(与脚手架 6 文件同等量级,手写即可)
- B:基础目录结构(如果 A 选了 FastAPI,`api/` 落点会带出)
- C:Phase 0 第 1 项子目录盘点(脚手架落地后可以做"先有位置可写"的前置盘点)
- D:`工程顾问开发架构与计划.md` 入库决策(等 T1 决定合并策略后再做)

A 是主计划明确 8/27 的项,继续按日推进。

## 备注

- 本次 commit 主题:`scaffold: Next.js 14 App Router 脚手架 v0.1 - 6 文件手写(不跑 install)`
- `.plan/20260826.md` 入库后由 cron 流程第 9 步"删除临时 plan 文件"环节删掉,与历史 8/25 一致
- 8/25 plan 提到"`.plan/20260824.md` 的 deleted 状态被本次 commit 自然消化"——今日已无该 untracked,继续观测
